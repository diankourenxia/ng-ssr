import { NormalDatePipe } from './normal-date.pipe';

describe('NormalDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NormalDatePipe();
    expect(pipe).toBeTruthy();
  });
});
